package controllers;

import models.MainEntity;
import models.Tag;
import models.User;

public class Tags {
	
	public static void unfollow(Tag tag, User user) {
		//tag.unfollow(user);
	}
}
